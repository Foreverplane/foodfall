
synced event SwitchGameState {
    GameState TargetState;
}
signal SwitchGameState(GameState targetState);

synced event PrepareGame{
    FP GameStartTime;
}

signal ChangePlayerScore(Int32 score, PlayerRef playerID);
